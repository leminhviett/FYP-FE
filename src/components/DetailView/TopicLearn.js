import React from "react";
import NormalizeContainer from "../NormalizeContainer";

const TopicLearn = () => {
	console.log("topic learn");
	return (
		<NormalizeContainer>
			<p>Learning topic</p>
			<p>Add to db learner progress</p>
		</NormalizeContainer>
	);
};

export default TopicLearn;
